<p id="notice"><%= notice %></p>
<%if can? :update, @student %>
  <%= link_to 'Edit', edit_student_path(@student) %>
<%end %>

<%if can? :send, @student %>
  <%= link_to 'Send message', mails_new_path(to: @email) %>
<%end %>
<%= link_to 'Back', students_path %>
